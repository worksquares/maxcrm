NODE_ENV=development
PORT=4000
API_PREFIX=/api

# PostgreSQL Database Configuration
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL=postgresql://maxcrm_user:maxcrm_password@localhost:5432/maxcrm

# JWT Authentication
JWT_SECRET=your-secret-key-change-in-production-with-random-string
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Database Setup Instructions:
# 1. Install PostgreSQL if not already installed
# 2. Create database: createdb maxcrm
# 3. Create user: createuser -P maxcrm_user (enter password: maxcrm_password)
# 4. Grant privileges: psql -c "GRANT ALL PRIVILEGES ON DATABASE maxcrm TO maxcrm_user;"
# 5. The API will automatically create tables and seed data on first run
#
# Alternative: Use Docker for PostgreSQL:
# docker run --name maxcrm-postgres -e POSTGRES_USER=maxcrm_user -e POSTGRES_PASSWORD=maxcrm_password -e POSTGRES_DB=maxcrm -p 5432:5432 -d postgres:15
